{
  "//": [
    "Module resolution for the SEALED acceptance tree (ms-3, #1544).",
    "",
    "The slice lives at repo-root `tests/acceptance/ms-3/`, while the only",
    "`node_modules` in the repo is under `coord/dashboard/webapp/`. Node's",
    "resolver walks UP from the importing file, and nothing above",
    "`tests/acceptance/` has a `node_modules`, so a bare",
    "`import ... from '@playwright/test'` fails with",
    "`Cannot find module '@playwright/test'` followed by `No tests found` —",
    "a wiring failure that reads exactly like an empty suite (the #1552",
    "failure mode, in Playwright form).",
    "",
    "Playwright picks up the tsconfig.json closest to each test file and honours",
    "its `paths`, so this maps the bare specifier back at the webapp's install.",
    "It is scoped to this directory: it cannot affect the webapp's own build",
    "(coord/dashboard/webapp/tsconfig.json only includes `src`).",
    "",
    "FRICTION, filed per #1544's standing-protocol clause: a sealed slice should",
    "not have to hand-roll module resolution. The durable fix belongs in",
    "`playwright.acceptance.config.ts` (a `tsconfig:` entry) or a",
    "`tests/acceptance/package.json` — both outside a test-author's writable",
    "surface, which is why it is worked around here rather than fixed here."
  ],
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "baseUrl": ".",
    "paths": {
      "@playwright/test": ["../../../coord/dashboard/webapp/node_modules/@playwright/test"],
      "@playwright/test/*": ["../../../coord/dashboard/webapp/node_modules/@playwright/test/*"]
    }
  },
  "include": ["*.spec.ts"]
}
