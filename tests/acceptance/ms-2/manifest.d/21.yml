# Acceptance manifest fragment for #21 (RPT-2: Reports rail item + picker +
# first report end-to-end, drive-queue-status) — milestone ms-2 "Reports
# panel", tracking issue #26.
#
# Per #2543, this file belongs to #21 alone; no other ms-2 slice writes here.
#
# Driver: web-playwright
#   npm run test:acceptance -- ms-2
#
# Test-ids are the exact shape coord.acceptance_drivers.parse_playwright_json_report
# derives from Playwright's built-in json reporter:
#   "[{project}] {file} › {describe path} › {test title}"
# `{file}` is relative to the report's rootDir (tests/acceptance/), hence the
# `ms-2/` prefix. Copied verbatim from a real
# `npm run test:acceptance -- ms-2 --reporter=json` run parsed by that same
# function — not hand-composed.
#
# Contract: tests/acceptance/ms-2/contract.md §1-§4, §6a-§6c, §7c/§7.1.
# Mocks: mocks/reports-picker.html (cold load), mocks/reports-grid.html
# (after Run report). Fixture: fixtures/reports-ms2.json (shared across the
# whole ms-2 milestone — see its own header comment).
#
# Deliberately NOT covered here (left for their own issues' slices, per
# contract.md's own issue table): Export CSV (§5, #24), row_identity Links on
# issue-activity/completed/decisions (§7, #23), the `decisions` options
# column (§6d, #22), chart rendering/Degrade (§8, #25), `money`-kind
# formatting (§6b, exercised only by `completed`, #22).

tests:
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › Reports rail entry is a ready, selected member of the Insight group": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › report picker lists exactly six tabs in the pinned order": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › drive-queue-status tab is selected on cold load, the other five are not": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › activating another tab moves the tablist selection": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › a description paragraph sits directly under the tablist": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › parameter bar renders a choice param as a select and a text param as an input": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › cold load shows the Reports header with no count and an empty-state placeholder": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › running drive-queue-status shows a header count of 3 rows": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › grid renders one header per column and one row per result": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › grid formats each row correctly per its ColumnMeta.kind": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › drive-queue-status Issue column has no row-nav link": 21
  "[chromium] ms-2/rpt-2-drive-queue-status.spec.ts › ms-2 Reports panel — RPT-2 drive-queue-status (#21) › the # column is sorted ascending by default, and clicking toggles the sort": 21

